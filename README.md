To set this project up run these commands:

Requirements:
    -Composer
    -Nodejs
    
Run this commands after cloning the repository
    -composer install
    -php artisan migrate:fresh --seed
    -npm install 
    -npm run build
